请将以下用户偏好拆解为四个维度，并仅返回 JSON 对象：
{{"semantic": [...], "emotional": [...], "stagecraft": [...], "songs": [...]}}

维度定义：
- semantic：剧情逻辑、题材、叙事结构、节奏、立意等综合语义偏好
- emotional：共情、生命力、情绪张力、价值观共鸣等情感偏好
- stagecraft：舞美、灯光、服装、布景、舞台调度、视觉风格等偏好
- songs：歌曲旋律、词曲、编曲、唱段、唱腔等音乐偏好

要求：
- 每个维度最多 3 条短语；
- 只提取文本中有明确依据的信息，不要臆造；
- 若某维度缺失，返回空数组；
- 只输出 JSON，不要输出解释。

用户文本：
{user_text}
